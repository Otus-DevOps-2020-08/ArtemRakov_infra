---
dependency:
  name: galaxy
driver:
  name: vagrant
  provider:
    name: virtualbox
platforms:
  - name: instance
    box: ubuntu/xenial64
lint: ansible-lint
provisioner:
  name: ansible
verifier:
  name: testinfra
